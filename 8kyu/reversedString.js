function reversedString(str){
  return str.split('').reverse('').join('');
}

// split() split out string into an array of letters.
// reverse() reverse the order of the items in our array
// join() join the items in our array back into a string


let reversedString = str => str.split().reverse().join();